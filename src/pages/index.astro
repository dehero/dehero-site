---
import Page from '../components/Page.astro';
import { getDoc } from '../entities/docs';

const doc = await getDoc('index');

if (!doc) {
  return new Response(null, {
    status: 404,
    statusText: 'Not found',
  });
}
---

<Page doc={doc} />
